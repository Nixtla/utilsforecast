



# <kbd>module</kbd> `utilsforecast.grouped_array`






---



## <kbd>class</kbd> `GroupedArray`






### <kbd>method</kbd> `__init__`

```python
__init__(data: ndarray, indptr: ndarray)
```








---



### <kbd>method</kbd> `append`

```python
append(new: ndarray) → Tuple[ndarray, ndarray]
```

Appends each element of `new` to each existing group. Returns a copy. 

---



### <kbd>method</kbd> `append_several`

```python
append_several(
    new_sizes: ndarray,
    new_values: ndarray,
    new_groups: ndarray
) → Tuple[ndarray, ndarray]
```





---



### <kbd>classmethod</kbd> `from_sorted_df`

```python
from_sorted_df(
    df: Union[DataFrame, DataFrame],
    id_col: str,
    time_col: str,
    target_col: str
) → GroupedArray
```





---



### <kbd>method</kbd> `take`

```python
take(idxs: Sequence[int]) → Tuple[ndarray, ndarray]
```

Subset specific groups by their indices. 

---



### <kbd>method</kbd> `take_from_groups`

```python
take_from_groups(idx: Union[int, slice]) → Tuple[ndarray, ndarray]
```

Select a subset from each group. 


