



# <kbd>module</kbd> `utilsforecast.preprocessing`
Utilities for processing data before training/analysis 

**Global Variables**
---------------
- **pl**

---



## <kbd>function</kbd> `id_time_grid`

```python
id_time_grid(
    df: DataFrame,
    freq: Union[str, int],
    start: Union[str, int, date, datetime] = 'per_serie',
    end: Union[str, int, date, datetime] = 'global',
    id_col: str = 'unique_id',
    time_col: str = 'ds'
) → DataFrame
```

Generate all expected combiations of ids and times. 



**Args:**
 
 - <b>`df`</b> (pandas or polars DataFrame):  Input data 
 - <b>`freq`</b> (str or int):  Series' frequency 
 - <b>`start`</b> (str, int, date or datetime, optional):  Initial timestamp for the series.  * 'per_serie' uses each serie's first timestamp  * 'global' uses the first timestamp seen in the data  * Can also be a specific timestamp or integer, e.g. '2000-01-01', 2000 or datetime(2000, 1, 1)  Defaults to "per_serie". 
 - <b>`end`</b> (str, int, date or datetime, optional):  Initial timestamp for the series.  * 'per_serie' uses each serie's last timestamp  * 'global' uses the last timestamp seen in the data  * Can also be a specific timestamp or integer, e.g. '2000-01-01', 2000 or datetime(2000, 1, 1)  Defaults to "global". 
 - <b>`id_col`</b> (str, optional):  Column that identifies each serie. Defaults to 'unique_id'. 
 - <b>`time_col`</b> (str, optional):  Column that identifies each timestamp. Defaults to 'ds'. 



**Returns:**
 
 - <b>`pandas or polars DataFrame`</b>:  Dataframe with expected ids and times. 


---



## <kbd>function</kbd> `fill_gaps`

```python
fill_gaps(
    df: DataFrame,
    freq: Union[str, int],
    start: Union[str, int, date, datetime] = 'per_serie',
    end: Union[str, int, date, datetime] = 'global',
    id_col: str = 'unique_id',
    time_col: str = 'ds'
) → DataFrame
```

Enforce start and end datetimes for dataframe. 



**Args:**
 
 - <b>`df`</b> (pandas or polars DataFrame):  Input data 
 - <b>`freq`</b> (str or int):  Series' frequency 
 - <b>`start`</b> (str, int, date or datetime, optional):  Initial timestamp for the series.  * 'per_serie' uses each serie's first timestamp  * 'global' uses the first timestamp seen in the data  * Can also be a specific timestamp or integer, e.g. '2000-01-01', 2000 or datetime(2000, 1, 1)  Defaults to "per_serie". 
 - <b>`end`</b> (str, int, date or datetime, optional):  Initial timestamp for the series.  * 'per_serie' uses each serie's last timestamp  * 'global' uses the last timestamp seen in the data  * Can also be a specific timestamp or integer, e.g. '2000-01-01', 2000 or datetime(2000, 1, 1)  Defaults to "global". 
 - <b>`id_col`</b> (str, optional):  Column that identifies each serie. Defaults to 'unique_id'. 
 - <b>`time_col`</b> (str, optional):  Column that identifies each timestamp. Defaults to 'ds'. 



**Returns:**
 
 - <b>`pandas or polars DataFrame`</b>:  Dataframe with gaps filled. 




---

_This file was automatically generated via [lazydocs](https://github.com/ml-tooling/lazydocs)._
