



# <kbd>module</kbd> `utilsforecast.processing`




**Global Variables**
---------------
- **pl**

---



## <kbd>function</kbd> `to_numpy`

```python
to_numpy(df: Union[DataFrame, pl_DataFrame]) → ndarray
```






---



## <kbd>function</kbd> `counts_by_id`

```python
counts_by_id(
    df: Union[DataFrame, pl_DataFrame],
    id_col: str
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `maybe_compute_sort_indices`

```python
maybe_compute_sort_indices(
    df: Union[DataFrame, pl_DataFrame],
    id_col: str,
    time_col: str
) → Optional[ndarray]
```

Compute indices that would sort the dataframe 



**Args:**
 
 - <b>`df`</b> (pandas or polars DataFrame):  Input dataframe with id, times and target values. 



**Returns:**
 
 - <b>`numpy array or None`</b>:  Array with indices to sort the dataframe or None if it's already sorted. 


---



## <kbd>function</kbd> `assign_columns`

```python
assign_columns(
    df: Union[DataFrame, pl_DataFrame],
    names: Union[str, List[str]],
    values: Union[ndarray, Series, pl_Series, List[float]]
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `drop_columns`

```python
drop_columns(
    df: Union[DataFrame, pl_DataFrame],
    columns: Union[str, List[str]]
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `take_rows`

```python
take_rows(
    df: Union[DataFrame, pl_DataFrame, Series, pl_Series, ndarray],
    idxs: ndarray
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `filter_with_mask`

```python
filter_with_mask(
    df: Union[Series, pl_Series, DataFrame, pl_DataFrame, Index, ndarray],
    mask: Union[ndarray, Series, pl_Series]
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `is_nan`

```python
is_nan(s: Union[Series, pl_Series]) → Union[Series, pl_Series]
```






---



## <kbd>function</kbd> `is_none`

```python
is_none(s: Union[Series, pl_Series]) → Union[Series, pl_Series]
```






---



## <kbd>function</kbd> `is_nan_or_none`

```python
is_nan_or_none(s: Union[Series, pl_Series]) → Union[Series, pl_Series]
```






---



## <kbd>function</kbd> `match_if_categorical`

```python
match_if_categorical(
    s1: Union[Series, pl_Series, Index],
    s2: Union[Series, pl_Series]
) → Tuple[Union[Series, pl_Series], Union[Series, pl_Series]]
```






---



## <kbd>function</kbd> `vertical_concat`

```python
vertical_concat(
    dfs: List[Union[DataFrame, pl_DataFrame, Series, pl_Series]],
    match_categories: bool = True
) → Union[DataFrame, pl_DataFrame, Series, pl_Series]
```






---



## <kbd>function</kbd> `horizontal_concat`

```python
horizontal_concat(
    dfs: List[Union[DataFrame, pl_DataFrame]]
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `copy_if_pandas`

```python
copy_if_pandas(
    df: Union[DataFrame, pl_DataFrame],
    deep: bool = False
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `join`

```python
join(
    df1: Union[DataFrame, pl_DataFrame, Series, pl_Series],
    df2: Union[DataFrame, pl_DataFrame, Series, pl_Series],
    on: Union[str, List[str]],
    how: str = 'inner'
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `drop_index_if_pandas`

```python
drop_index_if_pandas(
    df: Union[DataFrame, pl_DataFrame]
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `rename`

```python
rename(
    df: Union[DataFrame, pl_DataFrame],
    mapping: Dict[str, str]
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `sort`

```python
sort(
    df: Union[DataFrame, pl_DataFrame],
    by: Optional[str, List[str]] = None
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `offset_times`

```python
offset_times(
    times: Union[Series, pl_Series, Index],
    freq: Union[int, str, BaseOffset],
    n: Union[int, ndarray]
) → Union[Series, pl_Series, Index]
```






---



## <kbd>function</kbd> `offset_dates`

```python
offset_dates(
    dates: Union[Series, pl_Series, Index],
    freq: Union[int, str, BaseOffset],
    n: Union[int, Series, pl_Series]
) → Union[Series, pl_Series, Index]
```






---



## <kbd>function</kbd> `time_ranges`

```python
time_ranges(
    starts: Union[Series, pl_Series, Index],
    freq: Union[int, str, BaseOffset],
    periods: int
) → Union[Series, pl_Series]
```






---



## <kbd>function</kbd> `repeat`

```python
repeat(
    s: Union[Series, pl_Series, Index, ndarray],
    n: Union[int, ndarray, Series, pl_Series]
) → Union[Series, pl_Series, Index, ndarray]
```






---



## <kbd>function</kbd> `cv_times`

```python
cv_times(
    times: ndarray,
    uids: Union[Series, pl_Series, Index],
    indptr: ndarray,
    h: int,
    test_size: int,
    step_size: int,
    id_col: str = 'unique_id',
    time_col: str = 'ds'
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `group_by`

```python
group_by(
    df: Union[Series, pl_Series, DataFrame, pl_DataFrame],
    by,
    maintain_order=False
)
```






---



## <kbd>function</kbd> `group_by_agg`

```python
group_by_agg(
    df: Union[DataFrame, pl_DataFrame],
    by,
    aggs,
    maintain_order=False
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `is_in`

```python
is_in(s: Union[Series, pl_Series], collection) → Union[Series, pl_Series]
```






---



## <kbd>function</kbd> `between`

```python
between(
    s: Union[Series, pl_Series],
    lower: Union[Series, pl_Series],
    upper: Union[Series, pl_Series]
) → Union[Series, pl_Series]
```






---



## <kbd>function</kbd> `fill_null`

```python
fill_null(
    df: Union[DataFrame, pl_DataFrame],
    mapping: Dict[str, Any]
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `cast`

```python
cast(s: Union[Series, pl_Series], dtype: type) → Union[Series, pl_Series]
```






---



## <kbd>function</kbd> `value_cols_to_numpy`

```python
value_cols_to_numpy(
    df: Union[DataFrame, pl_DataFrame],
    id_col: str,
    time_col: str,
    target_col: Optional[str]
) → ndarray
```






---



## <kbd>function</kbd> `make_future_dataframe`

```python
make_future_dataframe(
    uids: Union[Series, pl_Series],
    last_times: Union[Series, pl_Series, Index],
    freq: Union[int, str, BaseOffset],
    h: int,
    id_col: str = 'unique_id',
    time_col: str = 'ds'
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `anti_join`

```python
anti_join(
    df1: Union[DataFrame, pl_DataFrame],
    df2: Union[DataFrame, pl_DataFrame],
    on: Union[str, List[str]]
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `ensure_sorted`

```python
ensure_sorted(
    df: Union[DataFrame, pl_DataFrame],
    id_col: str,
    time_col: str
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>function</kbd> `process_df`

```python
process_df(
    df: Union[DataFrame, pl_DataFrame],
    id_col: str,
    time_col: str,
    target_col: Optional[str]
) → ProcessedDF
```

Extract components from dataframe 



**Args:**
 
 - <b>`df`</b> (pandas or polars DataFrame):  Input dataframe with id, times and target values. 



**Returns:**
 
 - <b>`ProcessedDF`</b>:  A named tuple containing: 
        - ids (pandas or polars Series): serie with the sorted unique ids present in the data. 
        - last_times (numpy array): array with the last time for each serie. 
        - data (numpy ndarray): 2d array with target plus features values. 
        - indptr (numpy ndarray): 1d array with indices to the start and end of each serie. 
        - sort_idxs (numpy array or None): array with the indices that would sort the original data.  If the data is already sorted this is `None`. 


---



## <kbd>function</kbd> `backtest_splits`

```python
backtest_splits(
    df: Union[DataFrame, pl_DataFrame],
    n_windows: int,
    h: int,
    id_col: str,
    time_col: str,
    freq: Union[int, str, BaseOffset],
    step_size: Optional[int] = None,
    input_size: Optional[int] = None,
    allow_partial_horizons: bool = False
) → Generator[Tuple[Union[DataFrame, pl_DataFrame], Union[DataFrame, pl_DataFrame], Union[DataFrame, pl_DataFrame]], NoneType, NoneType]
```






---



## <kbd>function</kbd> `add_insample_levels`

```python
add_insample_levels(
    df: Union[DataFrame, pl_DataFrame],
    models: List[str],
    level: List[Union[int, float]],
    id_col: str = 'unique_id',
    target_col: str = 'y'
) → Union[DataFrame, pl_DataFrame]
```






---



## <kbd>class</kbd> `ProcessedDF`
ProcessedDF(uids, last_times, data, indptr, sort_idxs) 





---



## <kbd>class</kbd> `DataFrameProcessor`






### <kbd>method</kbd> `__init__`

```python
__init__(id_col: str = 'unique_id', time_col: str = 'ds', target_col: str = 'y')
```








---



### <kbd>method</kbd> `process`

```python
process(
    df: Union[DataFrame, pl_DataFrame]
) → Tuple[Union[Series, pl_Series], ndarray, ndarray, ndarray, Optional[ndarray]]
```








---

_This file was automatically generated via [lazydocs](https://github.com/ml-tooling/lazydocs)._
